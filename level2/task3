#include <iostream>
using namespace std;
int main() {
    int a,b,c;
    cout << "ededleri daxil et:";
    cin >> a >> b >> c;
    if (a >=b & a >= c)
    cout << "en boyuk=" << a;
    else if (b>=a & b>=c)
    cout << "en boyuk=" << b;
    else
    cout << "en boyuk=" << c;
    return 0;
}