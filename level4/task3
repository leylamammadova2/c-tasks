#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;

    for (int i = 2; i < N; i++) {
        bool ok = true;
        for (int j = 2; j * j <= i; j++) {
            if (i % j == 0) {
                ok = false;
                break;
            }
        }
        if (ok) cout << i << " ";
    }
}